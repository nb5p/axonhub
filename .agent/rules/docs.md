---
alwaysApply: false
globs: "docs/**/*.md"
---

# Documentation Rules

1. Change any file in `docs/` but not in `docs/solutions/` — should change zh and en file at the same time.
2. `docs/fork/` contains private-fork maintenance records. It is Chinese-only and is exempt from rule 1.
